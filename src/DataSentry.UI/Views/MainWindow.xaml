<Window x:Class="DataSentry.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:DataSentry.UI.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel}"
        Title="DataSentry" Height="260" Width="640">

    <!-- The scan screen proper is still to be designed. This is the one obvious thing to do — name a
         folder, scan it, read the headline — wired end to end through the view model. -->
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0"
                 Padding="6"
                 Text="{Binding FolderPath, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="Scan" Padding="16,6" Command="{Binding ScanCommand}" />
            <Button Content="Cancel" Padding="16,6" Margin="8,0,0,0" Command="{Binding CancelCommand}" />
        </StackPanel>

        <TextBlock Grid.Row="2"
                   Margin="0,20,0,0"
                   TextWrapping="Wrap"
                   Text="{Binding Status}" />
    </Grid>
</Window>
